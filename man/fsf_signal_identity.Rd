% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fsf-contract-stubs.R
\name{fsf_signal_identity}
\alias{fsf_signal_identity}
\title{Calculate FSF Signal Identity}
\usage{
fsf_signal_identity(data, tau = 0.5, group_cols = NULL)
}
\arguments{
\item{data}{A data frame containing canonical signed effects or validated
\code{up}, \code{down}, and \code{constant} states.}

\item{tau}{One positive finite numeric scalar used when \code{data} contains
signed effects. The default is \code{0.5}.}

\item{group_cols}{\code{NULL}, \code{character(0)}, or a character vector naming
columns that define independent grouping strata.}
}
\value{
One row per Signal Identity. Grouping columns occur first in
user-supplied order, followed by \code{feature_id}, counts, and probabilities.
Identities follow their first appearance in the input.
}
\description{
Aggregates feature-perturbation states into state counts and Signal Identity
probabilities independently within optional grouping strata.
}
\details{
If both \code{state} and \code{effect} columns are present, the validated
\code{state} column takes precedence and \code{effect} is not used. Columns other
than grouping columns and the required identity inputs are not propagated
to the aggregated output. State probabilities are the corresponding state
counts divided by each identity's own \code{n_perturbations}.
}
\section{Errors}{

Errors on invalid effect/state input, grouping specifications, identifiers,
duplicate observation keys, empty input, or invalid \code{tau}.
}

\examples{
effects <- data.frame(
  condition = c("A", "A", "B", "B"),
  feature_id = "gene1",
  perturbation_id = c("p1", "p2", "p1", "p2"),
  effect = c(1, 0, -1, -1)
)
fsf_signal_identity(effects, tau = 0.5, group_cols = "condition")

}
