# FSF v1 Annotation Authority and Provenance Contract

## 1. Scope

This document fixes the source authority and provenance contract for all
annotation-dependent, real-data FSF v1 products. It governs annotation joins,
representative-feature tables, enrichment inputs, biological summaries,
figures, tables, and manuscript products. It does not change the FSF v1
scientific definition or authorize regeneration of any downstream product.

The governing scientific lock is `docs/FSF_V1_CURRENT_SCIENTIFIC_LOCK.md`,
SHA-256:

`f3782d3fa2c6dc6b67df1784695934f78e9a570391f4f3a26e5b645d5515023c`

## 2. FSF Mathematical Authority

The sole real-data FSF mathematical and classification authority is:

`results/current_fsf_v1/current_fsf_feature_metrics.tsv`

SHA-256:

`f0874a99572884064d52ae0a77af7f4fe95863c1e8776be505fcd358827b095e`

It is authoritative for these fields:

- `condition`
- `feature_id`
- `n_perturbations`
- `n_up`
- `n_down`
- `n_const`
- `p_up`
- `p_down`
- `p_const`
- `dominant_state`
- `ssi`
- `stability_region`
- `signal_class`
- `stability_deviation`

No downstream annotation, enrichment, table, or figure script may
independently recompute these fields.

## 3. Biological Annotation Authority

The sole reusable source for biological annotation fields is the external
file:

`FSF_v1_annotation_master.tsv`

SHA-256:

`25627cfcaf544dd2793d9fd2462772d9cb76f4728bbcab8c9d29599520f5d584`

Its current local locator is:

`/media/saji/5E06441D0643F5152/FSF_SIGNAL_STRATIFICATION/results/real_data/annotation_master/FSF_v1_annotation_master.tsv`

The absolute path is only a local locator. The scientific identity of the
source is its filename, SHA-256, schema, and documented provenance.

## 4. Master Schema and Key Contract

The audited master has 15,187 rows and 36 columns. It has 15,187 unique
`feature_id` values, zero duplicated feature IDs, and zero missing or blank
feature IDs. It contains no FSF-derived classification field.

Its complete ordered schema is:

1. `feature_id`
2. `locus_tag`
3. `protein_id`
4. `protein_length`
5. `translation_status`
6. `eggnog_annotated`
7. `eggnog_seed_ortholog`
8. `eggnog_evalue`
9. `eggnog_score`
10. `eggnog_ogs`
11. `eggnog_max_annot_lvl`
12. `eggnog_cog_category`
13. `eggnog_description`
14. `eggnog_preferred_name`
15. `eggnog_go`
16. `eggnog_ec`
17. `eggnog_kegg_ko`
18. `eggnog_kegg_pathway`
19. `eggnog_kegg_module`
20. `eggnog_pfam`
21. `interpro_annotated`
22. `interpro_analyses`
23. `interpro_signature_accessions`
24. `interpro_signature_descriptions`
25. `interpro_ids`
26. `interpro_descriptions`
27. `interpro_go`
28. `interpro_pathways`
29. `n_interpro_rows`
30. `pfam_annotated`
31. `pfam_domains`
32. `pfam_accessions`
33. `pfam_descriptions`
34. `n_pfam_hits`
35. `annotation_source_count`
36. `annotation_status`

The canonical annotation join key is `feature_id`. Duplicate, missing, or
blank feature IDs invalidate the annotation source and must not be aggregated
or repaired silently.

## 5. Join Contract

The current annotation atlas must be formed as an in-memory left join:

`current_fsf_feature_metrics.tsv LEFT JOIN FSF_v1_annotation_master.tsv BY feature_id`

The audited cardinality is:

- Current identities: 91,122.
- Joined rows: 91,122.
- Unique `condition` and `feature_id` keys: 91,122.
- Duplicate composite keys: 0.
- Row multiplication: 0.
- Row loss: 0.
- Unmatched condition-feature identities: 0.
- Unmatched unique features: 0.

The FSF columns must be copied directly from the mathematical authority. The
35 non-key annotation columns must be appended from the biological annotation
authority without recomputing any FSF field.

## 6. Annotation Coverage

The following are feature-level annotation properties, not FSF class-derived
findings:

| Coverage property | Features | Denominator | Proportion |
|---|---:|---:|---:|
| Any annotation | 13,467 | 15,187 | 0.886745242641733 |
| eggNOG | 9,811 | 15,187 | 0.646013037466254 |
| InterPro | 12,480 | 15,187 | 0.8217554487390532 |
| Pfam | 11,834 | 15,187 | 0.7792190689405413 |
| GO | 9,321 | 15,187 | 0.61374860077698 |
| KEGG | 7,178 | 15,187 | 0.4726410746032791 |
| Unannotated | 1,720 | 15,187 | 0.11325475735826694 |

At least two annotation layers are present for 11,253 features. All three
annotation layers are present for 9,405 features.

## 7. Underlying Resource Provenance

The integrated master was generated by
`scripts/pipeline/28_build_integrated_annotation_master.R` from these external
resources:

| Resource | SHA-256 |
|---|---|
| `results/real_data/annotation_proteins/FSF_v1_clean_annotation_universe_proteins.tsv` | `eeb3129eab411dd7bf2c1f5463ffd815a5cb520b5ad3eb26bd8168308e9b8e6c` |
| `results/real_data/functional_annotation/eggnog/FSF_v1_eggnog.emapper.annotations` | `43771806088003222c954c63821b779478138c85fdc284b401b18725849c7e27` |
| `results/real_data/functional_annotation/interproscan/FSF_v1_clean_annotation_universe_proteins_noSTOP.faa.tsv` | `5e9ac5f80bb1d20350c909fc510745ec79954e1b9deb0edad859739160f7c3a1` |
| `results/real_data/functional_annotation/pfam/FSF_v1_pfam.domtblout` | `ea0f1085eab98d1af6ffe3e0da1da4a160573874d25750eb56d33e0449082699` |

These resources are provenance support. They are not copied or regenerated by
this contract.

## 8. Historical Atlas Serialization Proof

The historical pipeline documented in
`scripts/pipeline/29_class_annotation_summary.R` used:

`readr::read_tsv()` -> `dplyr::left_join(by = "feature_id")` -> `readr::write_tsv()`

An audited readr implementation reproduced the documented serialization
derivative exactly. This does not establish which readr version was installed
when the historical atlas was originally generated.

The round-trip audit established:

- `MASTER_FEATURES = 15187`
- `HIST_ROWS = 91122`
- `ANNOTATION_KEY_MISSING = 0`
- `ROUNDTRIP_EXACT_ROWS = 91122`
- `ROUNDTRIP_MISMATCH_ROWS = 0`

Raw `eggnog_evalue` differences were fully reproduced by R double parsing and
readr serialization. All affected e-value rows were reproduced, with zero
unexplained e-value differences. InterPro quote-multiplicity differences were
also fully reproduced by the historical readr serialization behavior, with
zero unexplained InterPro differences.

The raw master and historical atlas are not asserted to be identical. The
historical atlas annotation block is a deterministic serialization derivative
of the external master under the documented pipeline.

## 9. Historical Atlas Exclusion

The historical file `FSF_condition_class_annotation_master.tsv`, identified
by SHA-256
`ea9ce70db9ca0812287c7cf0b3ce098cf35d864c8d1d3d38b662ada06b0f53b8`,
is provenance only.

It must not supply current FSF mathematical or class fields and must not become
the biological annotation authority. Historical stability levels, dominant
identities, signal classes, and condition classes must be excluded from all
current products.

## 10. Large-File Storage and Distribution Policy

The 203,946,583-byte annotation master is not committed as a normal Git blob
because of its size. The 1,235,282,601-byte historical atlas is not imported.

Every future use of the external annotation master must verify its SHA-256
before analysis. A matching local path without a matching hash is insufficient.
No downstream workflow may silently substitute a similarly named file.

A later publication or release phase may distribute the master through an
appropriate data archive or release asset. This contract establishes no DOI,
archive record, or release location.

## 11. Downstream Usage Rules

Downstream workflows may join annotations, select subsets, summarize, create
gene sets, perform enrichment, and plot results. They must:

- Verify the annotation-master SHA-256 before reading it.
- Join annotation by `feature_id` only after confirming key uniqueness.
- Preserve the current `condition` plus `feature_id` identity key.
- Preserve all authoritative FSF values without recalculation or rounding.
- Reject row multiplication, row loss, or unmatched current identities.
- Exclude all historical FSF-derived fields.
- Record both source hashes in generated-product provenance.

## 12. Authority Order

For annotation-dependent current real-data products, authority is:

1. `docs/FSF_V1_CURRENT_SCIENTIFIC_LOCK.md` for scientific definitions.
2. `results/current_fsf_v1/current_fsf_feature_metrics.tsv` for all real-data
   FSF mathematical and classification fields.
3. `FSF_v1_annotation_master.tsv`, identified by its locked SHA-256 and
   schema, for biological annotation fields.
4. The four underlying annotation resources for generation provenance.
5. `FSF_condition_class_annotation_master.tsv` only as historical provenance.

No lower-ranked source may override or recompute a field governed by a
higher-ranked authority.
